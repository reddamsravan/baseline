{
  "name": "baseline-design-system",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.29.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  }
}
